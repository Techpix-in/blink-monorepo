{
  "name": "blink-monorepo",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "npm run dev --workspace=blink --workspace=blink-client",
    "build": "npm run build --workspace=blink --workspace=blink-client",
    "dev:client": "npm run dev --workspace=blink-client",
    "dev:server": "npm run dev --workspace=blink",
    "start:client": "npm run start --workspace=blink-client",
    "start:server": "npm run start --workspace=blink"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/blink",
    "packages/blink-client"
  ]
}
